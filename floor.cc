#include <vector>
#include "floor.h"
#include <iostream>
using namespace std;

Floor::Floor(string fileName):length{0}, height{0}{
   ifilestream fs {fileName};
   string line;
   
   while(getline(cin, line)){
      height++;
      length = line.length();
      for(int i=0; i< )
      
      
   }
}
